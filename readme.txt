at first 
download project from repository
at second
open in vs cod and write in terminal "export PATH=$PATH:$(go env GOPATH)/bin" then write "make run" in terminal
u can tets project in web - browser localhost:8080/docs